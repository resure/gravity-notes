dist
coverage
package-lock.json
src-tauri/target
src-tauri/gen

# Local, machine-specific Claude Code settings (gitignored) — not part of the codebase to format.
.claude/settings.local.json

# Design handoff bundles (HTML prototypes, notes) — reference artifacts, not source to format.
handoffs
design_handoff_notes_list
