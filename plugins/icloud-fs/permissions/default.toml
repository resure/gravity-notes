"$schema" = "schemas/schema.json"

[default]
description = "Allows picking an iOS folder and resolving its saved security-scoped bookmark."
permissions = ["allow-pick-folder", "allow-resolve-bookmark"]
