"$schema" = "schemas/schema.json"

[default]
description = "Allows picking an iOS folder, resolving its saved bookmark, and coordinated per-note read/write."
permissions = [
  "allow-pick-folder",
  "allow-resolve-bookmark",
  "allow-read-note",
  "allow-write-note",
]
